# Governance-Security-Hardening-AzureRBAC
create a new User in Microsoft Entra ID, assign them limited permissions (RBAC), enforce rules using Azure Policy to prevent expensive deployments, and set up a Budget to alert you on spending.
